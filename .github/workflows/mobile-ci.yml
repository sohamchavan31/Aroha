name: Mobile CI

on:
  push:
    branches:
      - dev
      - 'feature/**'
    paths:
      - 'nira-mobile/**'
  pull_request:
    branches:
      - dev
      - main
    paths:
      - 'nira-mobile/**'

jobs:
  check:
    name: Install & Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nira-mobile

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: nira-mobile/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check Expo config is valid
        run: npx expo config --type public
